# Find strongly connected component (Kosaraju's algorithm)
```c++
#if 0
// definition of Graph
class Graph {
    std::vector<int> vertex;
    Graph()
}

#endif

void std::vector<std::vector<int>>findStronglyConnectedComponent(Graph graph) {
    for (graph)
}

```